# Core application
NODE_ENV=development
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace-with-random-32-char-secret
JWT_SECRET=replace-with-random-32-char-secret

# Database
POSTGRES_PASSWORD=mediaplanpro_dev
DATABASE_URL=postgresql://mediaplanpro:${POSTGRES_PASSWORD}@postgres:5432/mediaplanpro

# OpenAI (required for strategy generation)
OPENAI_API_KEY=sk-your-openai-api-key

# AWS S3 (replace if you need file uploads locally)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
AWS_S3_BUCKET=mediaplanpro-files

# Redis
REDIS_URL=redis://redis:6379

# Optional monitoring
SENTRY_DSN=
SENTRY_ENVIRONMENT=development

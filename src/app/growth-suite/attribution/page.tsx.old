'use client';

import { useState } from 'react';
import Link from 'next/link';
import { GitBranch, Download, Calendar, TrendingUp, DollarSign, Users } from 'lucide-react';

// Mock data - will be replaced with API call
const mockStats = {
  totalSessions: 12453,
  totalConversions: 342,
  totalRevenue: 45600,
  conversionRate: 2.75,
};

const mockTopChannels = [
  { channel: 'Organic Search', sessions: 4521, conversions: 142, revenue: 18900, cvr: 3.14 },
  { channel: 'Paid Search', sessions: 3210, conversions: 98, revenue: 13200, cvr: 3.05 },
  { channel: 'Social Media', sessions: 2145, conversions: 54, revenue: 7200, cvr: 2.52 },
  { channel: 'Direct', sessions: 1876, conversions: 32, revenue: 4300, cvr: 1.71 },
  { channel: 'Email', sessions: 701, conversions: 16, revenue: 2000, cvr: 2.28 },
];

const attributionModels = [
  { id: 'first-touch', name: 'First Touch', description: 'All credit to first interaction' },
  { id: 'last-touch', name: 'Last Touch', description: 'All credit to last interaction' },
  { id: 'linear', name: 'Linear', description: 'Equal credit to all touchpoints' },
  { id: 'position-based', name: 'Position-Based', description: '40% first, 40% last, 20% middle' },
  { id: 'time-decay', name: 'Time Decay', description: 'More credit to recent touchpoints' },
];

export default function AttributionPage() {
  const [selectedModel, setSelectedModel] = useState('first-touch');
  const [dateRange, setDateRange] = useState('30d');

  return (
    <div className="min-h-screen bg-bg-primary py-12 px-4 sm:px-6 lg:px-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 bg-bg-secondary border border-border-primary rounded-lg p-8 animate-fade-in-up">
          <div className="flex items-center justify-between">
            <div>
              <div className="flex items-center mb-4">
                <div className="w-12 h-12 rounded-lg flex items-center justify-center mr-4 bg-[#F59E0B]">
                  <GitBranch className="h-6 w-6 text-black" />
                </div>
                <h1 className="text-4xl font-bold text-text-primary">
                  Attribution Explorer
                </h1>
              </div>
              <p className="text-lg text-text-secondary">
                Understand your customer journey with multi-touch attribution
              </p>
            </div>
            <div className="flex gap-3">
              <select
                value={dateRange}
                onChange={(e) => setDateRange(e.target.value)}
                className="px-4 py-2 rounded-lg border border-border-primary bg-bg-tertiary text-text-primary focus:ring-2 focus:ring-[#F59E0B] focus:border-transparent"
              >
                <option value="7d">Last 7 days</option>
                <option value="30d">Last 30 days</option>
                <option value="90d">Last 90 days</option>
              </select>
              <button className="bg-bg-tertiary border border-border-primary text-text-primary hover:bg-bg-hover rounded-lg px-4 py-2 flex items-center gap-2 transition-all">
                <Download className="h-5 w-5" />
                Export
              </button>
            </div>
          </div>
        </div>

        {/* Stats Cards */}
        <div className="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
          <div className="bg-bg-tertiary border border-border-primary rounded-lg p-6 stagger-1 hover:border-border-hover transition-all">
            <div className="flex items-center justify-between mb-2">
              <span className="text-sm font-medium text-text-secondary">
                Total Sessions
              </span>
              <Users className="h-5 w-5 text-[#F59E0B]" />
            </div>
            <div className="text-3xl font-bold text-text-primary">
              {mockStats.totalSessions.toLocaleString()}
            </div>
          </div>

          <div className="bg-bg-tertiary border border-border-primary rounded-lg p-6 stagger-2 hover:border-border-hover transition-all">
            <div className="flex items-center justify-between mb-2">
              <span className="text-sm font-medium text-text-secondary">
                Conversions
              </span>
              <TrendingUp className="h-5 w-5 text-[#F59E0B]" />
            </div>
            <div className="text-3xl font-bold text-text-primary">
              {mockStats.totalConversions.toLocaleString()}
            </div>
          </div>

          <div className="bg-bg-tertiary border border-border-primary rounded-lg p-6 stagger-3 hover:border-border-hover transition-all">
            <div className="flex items-center justify-between mb-2">
              <span className="text-sm font-medium text-text-secondary">
                Revenue
              </span>
              <DollarSign className="h-5 w-5 text-[#F59E0B]" />
            </div>
            <div className="text-3xl font-bold text-text-primary">
              ${mockStats.totalRevenue.toLocaleString()}
            </div>
          </div>

          <div className="bg-bg-tertiary border border-border-primary rounded-lg p-6 hover:border-border-hover transition-all">
            <div className="flex items-center justify-between mb-2">
              <span className="text-sm font-medium text-text-secondary">
                CVR
              </span>
              <Calendar className="h-5 w-5 text-[#F59E0B]" />
            </div>
            <div className="text-3xl font-bold text-text-primary">
              {mockStats.conversionRate}%
            </div>
          </div>
        </div>

        {/* Attribution Model Selector */}
        <div className="bg-bg-secondary border border-border-primary rounded-lg p-6 mb-8">
          <h2 className="text-xl font-semibold mb-4 text-text-primary">
            Attribution Model
          </h2>
          <div className="grid grid-cols-1 md:grid-cols-5 gap-4">
            {attributionModels.map((model) => (
              <button
                key={model.id}
                onClick={() => setSelectedModel(model.id)}
                className={`p-4 rounded-lg border-2 transition-all ${
                  selectedModel === model.id
                    ? 'border-[#F59E0B] bg-[#F59E0B]/10'
                    : 'border-border-primary hover:border-border-hover bg-bg-tertiary'
                }`}
              >
                <div className="font-semibold mb-1 text-text-primary">
                  {model.name}
                </div>
                <div className="text-xs text-text-tertiary">
                  {model.description}
                </div>
              </button>
            ))}
          </div>
        </div>

        {/* Top Channels */}
        <div className="bg-bg-secondary border border-border-primary rounded-lg p-8 mb-8">
          <h2 className="text-2xl font-bold mb-6 text-text-primary">
            Top Channels ({selectedModel.split('-').map(w => w.charAt(0).toUpperCase() + w.slice(1)).join(' ')})
          </h2>

          <div className="space-y-4">
            {mockTopChannels.map((channel, index) => (
              <div key={channel.channel} className="bg-bg-tertiary border border-border-primary rounded-lg p-6 hover:border-border-hover transition-all">
                <div className="flex items-center justify-between mb-4">
                  <h3 className="text-lg font-semibold text-text-primary">
                    {channel.channel}
                  </h3>
                  <span className="text-sm font-medium px-3 py-1 rounded-full bg-bg-elevated text-text-primary">
                    #{index + 1}
                  </span>
                </div>

                <div className="grid grid-cols-4 gap-4">
                  <div>
                    <div className="text-xs mb-1 text-text-tertiary">
                      Sessions
                    </div>
                    <div className="text-xl font-bold text-text-primary">
                      {channel.sessions.toLocaleString()}
                    </div>
                  </div>
                  <div>
                    <div className="text-xs mb-1 text-text-tertiary">
                      Conversions
                    </div>
                    <div className="text-xl font-bold text-text-primary">
                      {channel.conversions}
                    </div>
                  </div>
                  <div>
                    <div className="text-xs mb-1 text-text-tertiary">
                      Revenue
                    </div>
                    <div className="text-xl font-bold text-text-primary">
                      ${channel.revenue.toLocaleString()}
                    </div>
                  </div>
                  <div>
                    <div className="text-xs mb-1 text-text-tertiary">
                      CVR
                    </div>
                    <div className="text-xl font-bold text-text-primary">
                      {channel.cvr}%
                    </div>
                  </div>
                </div>

                {/* Progress bar */}
                <div className="mt-4">
                  <div className="w-full bg-bg-elevated rounded-full h-2">
                    <div className="h-2 rounded-full transition-all duration-300 bg-[#F59E0B]"
                      style={{
                        width: `${(channel.sessions / mockStats.totalSessions) * 100}%`
                      }}>
                    </div>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* Journey Visualization Placeholder */}
        <div className="bg-bg-secondary border border-border-primary rounded-lg p-8">
          <h2 className="text-2xl font-bold mb-4 text-text-primary">
            Customer Journey Flow
          </h2>
          <div className="bg-bg-elevated rounded-lg p-12 text-center">
            <GitBranch className="h-16 w-16 mx-auto mb-4 text-text-tertiary" />
            <p className="text-text-secondary">
              Sankey diagram visualization coming soon
            </p>
          </div>
        </div>

        {/* Free Tier Notice */}
        <div className="mt-8 bg-bg-tertiary border border-border-primary rounded-lg p-6">
          <div className="flex items-start">
            <GitBranch className="h-5 w-5 mr-3 flex-shrink-0 mt-0.5 text-[#F59E0B]" />
            <div>
              <h4 className="font-semibold mb-1 text-text-primary">
                Free Tier Limits
              </h4>
              <p className="text-sm text-text-secondary">
                Track up to 50,000 events per month with 90-day data retention.
                <Link href="/pricing" className="ml-1 underline text-[#F59E0B] hover:text-[#D97706]">
                  Upgrade for more
                </Link>
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}

